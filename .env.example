# Configurazione Cloudflare Worker Secrets
# NON committare il file .env con i valori reali!
# Questi valori vanno salvati con: npx wrangler secret put NOME_SECRET

# === OAuth ClickUp ===
# Ottieni questi da https://app.clickup.com/settings/apps
CLICKUP_CLIENT_ID=your_client_id_here
CLICKUP_CLIENT_SECRET=your_client_secret_here

# Ottieni questi completando il flusso OAuth via /oauth/start
CLICKUP_ACCESS_TOKEN=your_access_token_here
CLICKUP_REFRESH_TOKEN=your_refresh_token_here

# === Sicurezza Proxy ===
# Scegli una stringa random forte (es: openssl rand -hex 32)
MCP_API_KEY=your_random_api_key_here

# === Opzionale ===
# URL del worker (per debugging)
# WORKER_URL=https://clickup-mcp-proxy.your-subdomain.workers.dev
